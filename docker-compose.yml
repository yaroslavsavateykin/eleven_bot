services:
  app:
    build: .
    user: "1000:1000"
    env_file: .env
    volumes:
      - ./data:/data
    ports:
      - "8080:8080"
    healthcheck:
      test: ["CMD", "/app", "-healthcheck"]
      interval: 30s
      timeout: 3s
      retries: 3
