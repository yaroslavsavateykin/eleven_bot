services:
  app:
    build: .
    user: "1000:1000"
    env_file: .env
    volumes:
      - ./data:/data
    ports:
      - "6767:6767"
    healthcheck:
      test: ["CMD", "/app", "-healthcheck"]
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 40s
